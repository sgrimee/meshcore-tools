[project]
name = "meshcore-nodes"
version = "0.1.0"
description = "Meshcore node public key database and lookup tool"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
nodes = "nodes:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
include = ["nodes.py"]
